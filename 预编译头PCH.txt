
在VS建立C++/C工程文件的时候，总会自动生成预编译文件#include "pch.h" ，其目的是为了提高编译效率。把一个工程中较稳定的代码预先编译好放在(pch.h)文件里，避免每次编译时去重新编译没有修改的所有文件。那么，有时候建立一些比较小的工程文件的时候，不需要预编译存在，虽然放着也没啥影响，但是看着别扭。下面就来说一下怎么取消预编译：
 

点击：项目——>(最后一栏）XXX属性——>配置属性——>C/C++——>预编译头——>预编译头——>选择不使用s